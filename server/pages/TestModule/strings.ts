import { StringsDefinition } from '../../@types/strings'

const strings: StringsDefinition = {
  errors: {
    missingField: 'something',
  },
  pageHeadings: {
    testPage: 'Test Page Heading',
  },
}

export default strings
