{% extends "../../partials/layout.njk" %}

{% set pageTitle = "Decision to recall" if recommendation.managerRecallDecision.selected.value == 'RECALL' else 'Decision not to recall' %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            {% include '../../partials/error-summary.njk' %}
            {% set html %}
                <div class='govuk-!-margin-top-1 govuk-!-margin-bottom-1 govuk-!-font-size-24'>{{ recommendation.crn }}</div>
                <div class='govuk-!-font-size-24'>{{ recommendation.personOnProbation.name }}</div>
            {% endset %}
            {{ govukPanel({
                titleText: pageTitle,
                html: html
            }) }}
            <h2 class='govuk-heading-m govuk-!-margin-top-6'>What happens next</h2>
            <p class='govuk-body'>You must contact the probation practitioner and tell them your decision.</p>
            <p class='govuk-body'>A 'Management oversight - recall' contact has been added to NDelius.</p>
            <p class='govuk-body govuk-!-margin-top-6'><a class='govuk-link' href='/cases/{{ recommendation.crn }}/overview'>Back to case overview</a></p>
        </div>
    </div>
{% endblock %}



