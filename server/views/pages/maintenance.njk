{% extends "../partials/layout.njk" %}

{% set pageTitle = makePageTitle({ pageHeading: "Maintenance page" }) %}
{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            {{ heading('Service temporarily unavailable', size = 'xl') }}
            {{ bodyParagraph('The system will be undergoing maintenance between ' + maintenancePageStartDateTime + ' and ' + maintenancePageEndDateTime) }}
            {{ bodyParagraph(maintenancePageBody) }}
        </div>
    </div>
{% endblock %}
