{% extends "../../partials/layout.njk" %}

{% set pageTitle = makePageTitle({ pageHeading: "How has the person responded to probation so far?", hasErrors: errors }) %}

{% block beforeContent %}
    {{ govukBackLink({
        text: "Back",
        href: '/cases/' + crn + '/overview'
    }) }}
{% endblock %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            {% include '../../partials/error-summary.njk' %}
            <form novalidate method="post">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                <div class="govuk-form-group">
                    {{ govukTextarea({
                        name: "behaviour",
                        id: "behaviour",
                        label: {
                            text: "How has " + personalDetailsOverview.name + " responded to probation so far?",
                            classes: "govuk-label--l",
                            isPageHeading: true
                        },
                        hint: {
                            text: "Give details of overall compliance. This includes good behaviour. Your answer will go into a Part A form if you decide to recall " + personalDetailsOverview.name + ". " + personalDetailsOverview.name + " will see the Part A."
                        },
                        value: recommendation.behaviour
                    }) }}
                </div>
                {{ formSubmitButton() }}
            </form>
        </div>
    </div>
{% endblock %}

