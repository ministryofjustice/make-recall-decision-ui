{% extends "../../partials/layout.njk" %}

{% set pageTitle = makePageTitle({ pageHeading: "Part A created", hasErrors: errors }) %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            {% set html %}
                {{ recommendation.recallType.text }}<br />
                <strong>{{ personalDetailsOverview.name }}</strong>
            {% endset %}
            {{ govukPanel({
                titleText: "Part A created",
                html: html
            }) }}
            <h2 class='govuk-heading-m govuk-!-margin-top-6'>What happens next</h2>
            <p class='govuk-body'><a class='govuk-link' href='/assets/Duncann-Cardours-PartA-SAMPLE.docx' target='_blank'>Download the Part A</a>. Then you can upload it to NDelius.</p>
            <p class='govuk-body'>These are at the end of the Part A:</p>
            <ul class='govuk-list govuk-list--bullet'>
                <li>OAsys R6.1</li>
                <li>OAsys R6.2</li>
                <li>OAsys R10</li>
            </ul>
        </div>
    </div>
{% endblock %}

