{% extends "../../partials/layout.njk" %}

{% set pageTitle = makePageTitle({ pageHeading: "What alternatives to recall have been tried already?", hasErrors: errors }) %}

{% block beforeContent %}
    {{ govukBackLink({
        text: "Back",
        href: '/rec-prototype/' + crn + '/licence-conditions'
    }) }}
{% endblock %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            {% include '../../partials/error-summary.njk' %}
            <form novalidate method="post">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                <div class="govuk-form-group">
                    <fieldset class="govuk-fieldset" aria-describedby="contact-hint">
                        <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
                            <h1 class="govuk-fieldset__heading">
                                What alternatives to recall have been tried already?
                            </h1>
                        </legend>
                        <div id="contact-hint" class="govuk-hint">
                            Give full details, including dates where relevant.
                        </div>
                        <div class="govuk-checkboxes" data-module="govuk-checkboxes">
                            <div class="govuk-checkboxes__item">
                                <input class="govuk-checkboxes__input" id="alternativesTried-NONE" name="alternativesTried" type="checkbox" value="NONE" data-behaviour="exclusive" data-aria-controls="conditional-NONE">
                                <label class="govuk-label govuk-checkboxes__label" for="alternativesTried-NONE">
                                    None
                                </label>
                            </div>
                            <div class="govuk-checkboxes__conditional govuk-checkboxes__conditional--hidden" id="conditional-NONE">
                                {{ govukTextarea({
                                    name: "alternativesTriedDetail-NONE",
                                    id: "alternativesTriedDetail-NONE",
                                    label: {
                                        text: "Explain why"
                                    },
                                    value: ""
                                }) }}
                            </div>
                            <div class="govuk-checkboxes__divider">or</div>
                            {% for item in recommendation.alternativesTriedAllOptions %}
                            <div class="govuk-checkboxes__item">
                                <input class="govuk-checkboxes__input" id="option-{{ item.value }}" name="alternativesTried" type="checkbox" value="{{ item.value }}" data-aria-controls="conditional-{{ item.value }}" {% if item.checked %}checked{% endif %}>
                                <label class="govuk-label govuk-checkboxes__label" for="option-{{ item.value }}">
                                    {{ item.text }}
                                </label>
                            </div>
                            <div class="govuk-checkboxes__conditional govuk-checkboxes__conditional--hidden" id="conditional-{{ item.value }}">
                                {{ govukTextarea({
                                    name: "alternativesTriedDetail-" + item.value,
                                    id: "alternativesTriedDetail-" + item.value,
                                    label: {
                                        text: item.detailsLabel
                                    },
                                    value: item.detail
                                }) }}
                            </div>
                            {% endfor %}
                        </div>

                    </fieldset>
                </div>
                {{ formSubmitButton() }}
            </form>
        </div>
    </div>
{% endblock %}

