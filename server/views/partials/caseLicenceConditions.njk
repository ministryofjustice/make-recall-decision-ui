<h1 class='govuk-heading-m govuk-visually-hidden'>Licence conditions<span
            class='govuk-visually-hidden'> for {{ caseSummary.personalDetailsOverview.name }}</span></h1>
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">

        <!-- We display the banners based on active convictions in NDelius. CVL does NOT matter -->
        {% include './notificationBanners/convictionsLicencesBanner.njk' %}

        {% if  caseSummary.licenceConvictions.activeCustodial.length > 0 and caseSummary.hasAllConvictionsReleasedOnLicence %}
            {% if caseSummary.cvlLicence %}
                {% include './licenceConditionsCVL.njk' %}
            {% else %}
                {% include './licenceConditionsNDelius.njk' %}
            {% endif %}
        {% endif %}

    </div>
</div>
